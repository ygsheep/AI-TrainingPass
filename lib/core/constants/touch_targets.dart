/// Touch Target Sizes (WCAG 2.1 AAA Compliant)
/// Minimum touch target size: 44x44px
class TouchTargets {
  // Minimum touch size (WCAG 2.1 AAA)
  static const double minSize = 44.0;

  // Alias for compatibility
  static const double minimumSize = 44.0;

  // Recommended touch size
  static const double recommendedSize = 48.0;

  // Large buttons (easier to tap)
  static const double largeSize = 56.0;

  // Spacing between touch targets
  static const double spacing = 8.0;
  static const double spacingLarge = 16.0;

  // Padding values
  static const double paddingSmall = 8.0;
  static const double paddingMedium = 16.0;
  static const double paddingLarge = 24.0;

  // Icon button sizes
  static const double iconButtonSmall = 40.0;
  static const double iconButtonMedium = 48.0;
  static const double iconButtonLarge = 56.0;

  // Navigation bar height
  static const double navigationBarHeight = 60.0;
}
